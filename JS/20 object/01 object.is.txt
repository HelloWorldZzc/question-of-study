object.is方法是判断两个值是否为同一个值，只有满足一下条件的时候返回true，其他的条件都是false
    1）数字类型
        1、都非0数字且相等
        2、都是+0
        3、都是-0
        4、都是NaN
    2) 其他类型
        1）都是undefined
        2）都是null
        3）都是true 或false
        4）都是相同对象（地址相同）
        5）都是长度相同的字符且排序相同（str1===str2）
